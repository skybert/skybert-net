#+title: PostgreSQL Backup & Restore

* Backup
#+begin_src text
$ pg_dump -o <db> > /tmp/$(date --iso)-<db>.sql
#+end_src

* Restore

#+begin_src text
$ psql db < /tmp/$(date --iso)-<db>.sql
#+end_src
